# Network Intrusion Detection System (IDS)

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://python.org)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Scapy](https://img.shields.io/badge/Scapy-2.5.0+-yellow.svg)](https://scapy.net)

**Система виявлення мережевих атак**, що аналізує трафік з `.pcap` файлів та виявляє потенційні загрози на основі статистичних та сигнатурних правил. Проєкт реалізовано в межах дипломної роботи та демонструє модульну архітектуру аналізатора мережевого трафіку.

---

## 📌 Зміст
- [Мета системи](#-мета-системи)
- [Архітектура](#-архітектура-системи)
- [Принцип роботи](#-принцип-роботи)
- [Алгоритми виявлення](#-реалізовані-алгоритми-виявлення)
- [Налаштування](#-налаштування)
- [Запуск проєкту](#-запуск-проєкту)
- [Логування та сповіщення](#-логування-та-email-сповіщення)
- [Документування коду](#-документування-коду)
- [Як долучитися](#-як-долучитися-до-розробки)
- [Ліцензія](#-ліцензія)

---

## 🎯 Мета системи

Автоматизований аналіз мережевого трафіку з метою виявлення:

* **DoS (Denial of Service)** атак
* **Сканування портів (Port Scan)**
* **Brute Force** атак

---

## 🏗 Архітектура системи

Проєкт побудований за модульним принципом:
network_ids/
│
├── config.py # Налаштування системи
├── collector.py # Зчитування pcap-файлу
├── analyzer.py # Виділення параметрів пакетів
├── detector.py # Алгоритми виявлення атак
├── logger.py # Логування подій
├── notifier.py # Відправка email-сповіщень
└── main.py # Точка входу

text

---

## ⚙️ Принцип роботи

Система працює у наступній послідовності:

1. **Зчитування** мережевого трафіку з `.pcap` файлу
2. **Аналіз** кожного TCP/IP пакета
3. **Формування статистики** по IP-адресах
4. **Виявлення аномалій** за алгоритмами
5. **Логування** результатів
6. **Відправка email-сповіщення** адміністратору

---

## 🔍 Реалізовані алгоритми виявлення

### DoS атака
Виявляється при перевищенні порогової кількості пакетів від одного джерела у межах визначеного часового вікна.

**Критерій:** кількість пакетів > `DOS_THRESHOLD` за `TIME_WINDOW` секунд

### Port Scan
Виявляється, якщо одне джерело звертається до великої кількості різних портів.

**Критерій:** кількість унікальних портів > `PORTSCAN_THRESHOLD`

### Brute Force
Виявляється при великій кількості спроб підключення до сервісного порту (наприклад, SSH – порт 22).

**Критерій:** кількість звернень до порту 22 > `BRUTEFORCE_THRESHOLD`

---

## ⚙️ Налаштування

Усі параметри задаються в файлі `config.py`:

```python
# Файл з трафіком
PCAP_FILE = "/tshark/01.pcap"

# Порогові значення
DOS_THRESHOLD = 1000
PORTSCAN_THRESHOLD = 20
BRUTEFORCE_THRESHOLD = 15
TIME_WINDOW = 10

# Email-налаштування
SMTP_SERVER = "mail.example.com"
SMTP_PORT = 587
SMTP_USER = "admin@example.com"
SMTP_PASSWORD = "password"
ADMIN_EMAIL = "admin@example.com"
🚀 Запуск проєкту
Встановити залежності

bash
pip install scapy
Вказати шлях до pcap-файлу у config.py

python
PCAP_FILE = "/path/to/your/traffic.pcap"
Запустити систему

bash
python3 main.py
## 📋 Логування та Email-сповіщення
Логування
Результати роботи зберігаються у файл ids_log.txt:

text
[2026-03-05 12:30:01] DoS attack detected from 192.168.1.10
[2026-03-05 12:30:05] Port scan detected from 192.168.1.20
Email-сповіщення
При виявленні атаки система надсилає email адміністратору через SMTP-сервер (налаштовується в config.py).
```

## 📖 Документування коду

Щоб усі розробники писали код в єдиному стилі, ми використовуємо стандартні правила документування Python.

1. Загальні стандарти
Стандарт	Опис
PEP 257	Визначає, як писати docstrings (потрійні лапки """)
Google Style	Визначає структуру всередині docstrings
2. Як документувати
А. Модуль (файл)
python
"""
Модуль для збору мережевих пакетів з pcap файлу.
Використовує Scapy для читання трафіку.
"""
Б. Функція
python
def collect_packets():
    """
    Зчитує всі пакети з pcap файлу, вказаного в конфігурації.

    Returns:
        list: Список пакетів у форматі Scapy.
    """
    return rdpcap(PCAP_FILE)
В. Функція з аргументами
python
def log_event(message):
    """
    Записує повідомлення у лог-файл з часовою міткою.

    Args:
        message (str): Текст повідомлення для запису.
    """
    with open(LOG_FILE, "a") as f:
        f.write(f"[{datetime.now()}] {message}\n")
Г. Клас
python
class Detector:
    """
    Клас для виявлення мережевих атак на основі статистичних правил.

    Attributes:
        dos_threshold: Поріг для DoS атак.
        portscan_threshold: Поріг для сканування портів.
    """
3. Що обов'язково документувати
Тип елемента	Чи треба документувати
Файл (модуль)	✅ Так, завжди
Публічна функція	✅ Так, завжди
Приватна функція (_func)	❌ Ні (але можна при складній логіці)
Клас	✅ Так, завжди
Публічний метод класу	✅ Так
Приватний метод класу	❌ Ні
Змінна (константа)	❌ Ні (достатньо коментаря)
4. Інструменти для перевірки
bash
# Встановлення pdoc для генерації HTML документації
pip install pdoc

# Згенерувати документацію в папку html/
pdoc --html network_ids/
5. Приклади з нашого проєкту
Ми вже задокументували ключові модулі:

✅ collector.py – зчитування pcap

✅ logger.py – логування подій

✅ main.py – головний цикл роботи

## 👥 Як долучитися до розробки
Форкніть репозиторій

Створіть гілку для нової функціональності

Дотримуйтесь стандартів документування (розділ вище)

Зробіть pull request

Правила для комітів
Пишіть зрозумілі повідомлення

Додавайте теги для важливих етапів

Перед комітом переконайтесь, що код задокументовано

### 📄 Ліцензія
Цей проєкт розповсюджується під ліцензією MIT. Деталі у файлі LICENSE.


---

## 🚀 Документування розгортання (для нових розробників)

Цей розділ допоможе тобі швидко налаштувати проєкт на своєму комп'ютері з нуля.

### 1. Встановлення необхідного програмного забезпечення

#### Для Windows:
1. **Встанови Python** (версія 3.8 або новіша)
   - Завантаж з [python.org](https://www.python.org/downloads/)
   - Під час встановлення ОБОВ'ЯЗКОВО постав галочку **"Add Python to PATH"**

2. **Встанови Git**
   - Завантаж з [git-scm.com](https://git-scm.com/download/win)
   - Встановлюй з налаштуваннями за замовчуванням

3. **Встанови редактор коду (VS Code)**
   - Завантаж з [code.visualstudio.com](https://code.visualstudio.com/download)
   - Встановлюй з налаштуваннями за замовчуванням


#### Для Linux (Ubuntu/Debian):
```bash
# Оновлення пакетів
sudo apt update && sudo apt upgrade -y

# Встановлення Python та Git
sudo apt install python3 python3-pip git -y

# Встановлення VS Code (опціонально)
sudo snap install code --classic
```
---

Для macOS:

# Встановлення Homebrew (якщо ще не встановлено)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Встановлення Python та Git
brew install python3 git

# Встановлення VS Code
brew install --cask visual-studio-code

---

2. Клонування репозиторію

Відкрий термінал (Command Prompt, PowerShell або Terminal) та виконай:

# Перейди в папку, де хочеш зберігати проєкти
cd Desktop

# Склонуй репозиторій
git clone https://github.com/phantom82git/network_ids.git

# Перейди в папку проєкту
cd network_ids

---

3. Налаштування віртуального середовища (рекомендовано)
Створення віртуального середовища:

# Windows
python -m venv venv

# Linux/macOS
python3 -m venv venv

Активація віртуального середовища:

# Windows (Command Prompt)
venv\Scripts\activate

# Windows (PowerShell)
.\venv\Scripts\Activate

# Linux/macOS
source venv/bin/activate

Після активації в терміналі з'явиться (venv) на початку рядка.

---

4. Встановлення залежностей
Переконайся, що віртуальне середовище активоване, та виконай:

# Встановлення необхідних бібліотек
pip install scapy

Якщо в майбутньому з'являться додаткові залежності, їх можна буде встановити аналогічно.

---

5. Налаштування конфігурації
Відкрий файл config.py у текстовому редакторі та відредагуй параметри:

# Вкажи шлях до свого pcap-файлу
PCAP_FILE = "/шлях/до/твого/файлу.pcap"

# Порогові значення (можна залишити за замовчуванням)
DOS_THRESHOLD = 1000
PORTSCAN_THRESHOLD = 20
BRUTEFORCE_THRESHOLD = 15
TIME_WINDOW = 10

# Налаштування email (опціонально, якщо не потрібно - залиш як є)
SMTP_SERVER = "mail.example.com"
SMTP_PORT = 587
SMTP_USER = "admin@example.com"
SMTP_PASSWORD = "password"
ADMIN_EMAIL = "admin@example.com"

---

6. Запуск проєкту

Переконайся, що:

✅ Віртуальне середовище активоване

✅ Вказано правильний шлях до pcap-файлу в config.py

✅ Файл існує за вказаним шляхом

Тепер запусти проєкт:

    python3 main.py

Якщо все зроблено правильно, ти побачиш щось подібне:

🚀 Запуск системи виявлення атак...
📁 Читання файлу: traffic.pcap
✅ Прочитано пакетів: 1024
🔍 Аналіз пакетів...
🛡️ Виявлення атак...
⚠️ DoS attack detected from 192.168.1.10
📧 Відправлено сповіщень: 1


7. Базові команди для роботи
Робота з Git

# Перевірити статус змін
git status

# Додати всі зміни до коміту
git add .

# Зробити коміт
git commit -m "Опис змін"

# Завантажити зміни на GitHub
git push origin main

# Отримати останні зміни з GitHub
git pull origin main

---

9. Що далі?
Після успішного запуску ти можеш:

🔧 Змінювати порогові значення в config.py

📝 Додавати нові алгоритми виявлення в detector.py

📊 Аналізувати власні pcap-файли

Якщо виникнуть питання — пиши автору проєкту!

Автор: phantom82git
Контакти: andreymail428@gmail.com
Рік: 2026